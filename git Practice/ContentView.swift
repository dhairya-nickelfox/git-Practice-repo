//
//  ContentView.swift
//  git Practice
//
//  Created by Dhairya Bhardwaj on 12/12/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
            Text("git setUp")
            Text("change the code")
            Text("set up the code ")
            Text("set up the code ")
            Text("set up the code ")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
